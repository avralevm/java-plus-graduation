# Explore With Me

**ExploreWithMe (англ. «Исследуй со мной»)** — это приложение-афиша, позволяющее пользователям делиться информацией об интересных событиях и находить компанию для участия в них.  
Сервис помогает планировать свободное время, искать мероприятия и объединять людей с общими интересами.

---

## 🧩 Архитектура проекта

Проект реализован с использованием **микросервисной архитектуры** на базе **Spring Boot** и **Spring Cloud**.  
Основные сервисы взаимодействуют через **FeignClient**, а общие конфигурации управляются централизованно через **Config Server**.  
Для маршрутизации запросов используется **API Gateway**, а **Discovery Server (Eureka)** отвечает за регистрацию и поиск сервисов.

---

### 🔹 Основные сервисы:

* gateway-service — единая точка входа в систему. Осуществляет маршрутизацию запросов и взаимодействие с системой аутентификации и авторизации.

* config-service — хранит централизованные настройки для всех микросервисов.

* discovery-service — реализует сервис-регистрацию и обнаружение (Eureka).

* user-service — управляет пользователями: регистрация, активация, получение информации.

* event-service — обрабатывает события (создание, публикация, модерация, отмена).

* request-service — управляет заявками на участие в событиях.

* comment-service — реализует возможность оставлять и получать комментарии к событиям.

* stats-service — хранит статистику просмотров и обращений к API, предоставляет аналитические данные.

### 🔗 Взаимодействие между сервисами

- Все микросервисы регистрируются в **Eureka Discovery**.
- Взаимодействие происходит по REST через **FeignClient**.
- **Gateway** проверяет права доступа (публичная, закрытая, административная зоны API).
- **stats-service** принимает POST-запросы на запись статистики и отдает GET-запросами выборки данных.

---

## ⚙️ Конфигурация

- Централизованные конфигурации хранятся в **config-service**.
- Общие настройки БД, портов, Feign-клиентов и URL других сервисов определяются в `application.yml` каждого микросервиса.
- Подключение к БД и другим сервисам настраивается через Spring Cloud Config.
- Все сервисы используют **PostgreSQL** в качестве основной базы данных.
---

## 🐳 Запуск проекта

Проект использует **Docker** для контейнеризации сервисов и баз данных.  
Для локального запуска:

```bash
docker-compose up --build
```